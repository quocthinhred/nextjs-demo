/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['https://thegioithiep.com.vn/'],
  },
}

module.exports = nextConfig
